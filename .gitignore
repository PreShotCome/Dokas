/bin/
/dist/
/tmp/
*.exe
*.test
*.out

# Generated templ files (uncomment if you prefer to regenerate in CI)
# *_templ.go

# app.css (compiled Tailwind) and htmx.min.js are committed so a plain
# `git pull` + `go run ./cmd/server` serves correct styles without a build
# step. Rebuild app.css with `make css` after changing templates.

# Local env
.env
.env.local
.env.*.local

# Node
node_modules/

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# Goose embedded artifacts
/migrations/.goose_state
